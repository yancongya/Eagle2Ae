{
  "installCommand": "npx pnpm@10.12.4 --filter eagle2ae_web install --no-frozen-lockfile",
  "buildCommand": "npx pnpm@10.12.4 --filter eagle2ae_web run build",
  "outputDirectory": "apps/eagle2ae_web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
